import { Typography } from "@mui/material";

const Footer = () => {

    return (
        <div style={{ height: "100px", width: "100%", backgroundColor: "#ffd699", justifyContent: "center", display: "flex", alignItems: "center", flexDirection: "column" }}>
            <Typography variant="h5">E-Commerce</Typography>
            <Typography variant="subtitle1">One Stop for your need</Typography>
        </div>
    )

}

export default Footer;